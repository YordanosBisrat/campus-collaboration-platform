import 'package:flutter/material.dart';

class AppColors {
  // The primary peach/salmon color
  static const Color primary = Color(0xFFD69481);

  // Neutral colors for text and backgrounds
  static const Color background = Color(0xFFFFFBF8); // Creamy white from Figma
  static const Color textPrimary = Color(0xFF1A2A3A); // Dark navy for headings
  static const Color textSecondary = Color(0xFF7D8A95); // Grey for labels

  // Status colors
  static const Color error = Color(0xFFE57373);
  static const Color success = Color(0xFF81C784);
}
